if __name__ == "__main__":
    from AST.AST import *
    from AST.declares import *
else:
    from .AST.AST import *
    from .AST.declares import *

printLines([
    inta,
    intb,
    charc,
    chard,
    voidFuna.setbody(
    arrayInta
    .copy(arrayCharb)
    .copy(ASTExpression(operatorType="ADD", expression1=c, expression2=ASTExpression(operatorType="ARRAY",expression1=a,expression2=c)))  # c+a[c]
    .copy(ASTExpression(operatorType="MINUS", expression1=c, expression2=d))  # c-d
    .copy(ASTExpression(operatorType="MUL", expression1=c, expression2=d))  # c*d
    .copy(ASTExpression(operatorType="DIV", expression1=c, expression2=d))  # c/d
    .copy(ASTExpression(operatorType="AND", expression1=cLTd.copy(), expression2=cGTd.copy()))  # c<d&&c>d
    .copy(ASTExpression(operatorType="OR", expression1=cLTd.copy(), expression2=cGTd.copy()))  # c<d||c>d
    .copy(ASTExpression(operatorType="GT", expression1=c, expression2=d))  # c>d
    .copy(ASTExpression(operatorType="LT", expression1=c, expression2=d))  # c<d
    .copy(ASTExpression(operatorType="NE", expression1=c, expression2=d))  # c!=d
    .copy(ASTExpression(operatorType="GE", expression1=c, expression2=d))  # c>=d
    .copy(ASTExpression(operatorType="LE", expression1=c, expression2=d))  # c<=d
    .copy(ASTExpression(operatorType="NOT", expression1=cLTd.copy(), expression2=UNDEFINED))  # !(c<d)
    .copy(ASTExpression(operatorType="EQ", expression1=c, expression2=d))  # c==d
    .copy(ASTExpression(operatorType="ASSIGN", expression1=c.copy(), expression2=d.copy()))  # c=d
    .copy(ASTExpression(operatorType="ARRAY", expression1=a, expression2=c))  # a[c]
    .copy(ASTExpression(operatorType="ARRAY", expression1=b, expression2=cplusd)))  # b[d]
])
